#!/bin/sh

file="/tmp/ytvideo"

if [ -z "$1" ]; then
  url="$(xclip -o)"
else
  url="$1"
fi

yt-dlp --no-playlist -q -o "$file" "$url" >/dev/null 2>&1
mpv --terminal=no --resume-playback=no "$file"*
rm "$file"*
